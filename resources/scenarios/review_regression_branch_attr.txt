@<[mode=show;cond=get_var('opened');image=demo_media/rainy_bg]@<|
label("review_attr")
is_start()
set_var("opened", true)
branch([
    {"text": "属性兜底分支", "dest": "branch_show"},
    {"text": "显式覆盖分支（应忽略）", "dest": "branch_hidden", "cond": "false", "mode": "enable"}
])
|>
分支属性回归测试

@<| label("branch_show") |>
@<| is_chapter() |>
show("bg", "demo_media/rainy_bg")
show("fg", "demo_media/hero_sprite")
@<| is_end() |>

@<| label("branch_hidden") |>
这个分支理论上不会出现在选项里
@<| is_end() |>
